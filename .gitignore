**/.DS_Store
tikzjax.js

# Astro / Node
node_modules/
dist/
.astro/

# Local env — real R2 URL lives here; .env.example is the checked-in template
.env
.env.local

# Cloudflare API token for wrangler (secret) — never committed
.wrangler.env

